language: java
addons:
  sonarqube:
    organization: "java-crypto-examples" # the key of the org you chose at step #3
script:
  # other script steps might be done before running the actual SonarQube analysis
  - sonar-scanner
